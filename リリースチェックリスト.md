# 🚀 リリースまでにやること（チェックリスト）

①GA4 ②サブドメイン ③シェア画像 までできています。
**あと公開に必要なのは、下の4ステップだけ**です。難しい作業はありません。

---

## ✅ STEP 1. ホスティングにアップロード（置くだけ）
この `release` フォルダの中身を、静的ホスティングにアップロードします。
おすすめ（無料・簡単）：**Cloudflare Pages** / Netlify / Vercel のいずれか。
- フォルダをドラッグ＆ドロップするだけでURLが発行されます（例 `xxxx.pages.dev`）。
- `index.html` が入口です。

## ✅ STEP 2. サブドメイン shindan.mingleapp.jp をつなぐ
1. ホスティングの管理画面で「カスタムドメイン」に `shindan.mingleapp.jp` を追加。
2. 指定された向け先を、`mingleapp.jp` のDNSに登録（添付のレコード追加画面）：
   - ホスト名：`shindan` ／ TYPE：**CNAME** ／ VALUE：ホスティングが指定する向け先（例 `xxxx.pages.dev`）
   - （自前サーバの固定IPなら TYPE を **A**、VALUEにIP）
3. 数分〜で反映し、HTTPS（鍵マーク）も自動でつきます。
   → これで **https://shindan.mingleapp.jp** で診断が公開されます。

## ✅ STEP 3. GA4の測定IDを入れる（アクセス解析）
1. GA4で測定ID `G-XXXXXXXXXX` を発行（→ `GA4とドメイン設定ガイド.md` 参照）。
2. `config.js` の `ga4Id` に貼り付け → 再アップロード。
   → これで管理者だけが「タイプ別の多さ・離脱地点・登録到達」を見られます。

## ✅ STEP 4. 事前登録の集め方を決める（任意・あとででもOK）
メール登録を実際に手元に集めたい場合のみ：
- `config.js` の `subscribeEndpoint` に、フォーム受信サービス（Formspree等）のURLを入れる。
- 未設定でも診断・シェアは完全に動きます（メールはブラウザに保存されるだけ）。

---

## これで公開完了。動く機能（サーバー不要）
- 診断（ランディング→12問→分析→結果・全16タイプ）
- 結果ページ：メール登録で取扱説明書を解除／相性タイプ／16タイプ一覧
- **シェア**：X・LINEは**タイプ別の画像付きカード**（押すと診断トップへ）／Instagram用に縦画像を保存
- GA4でファネル計測（管理者のみ）

## シェアの仕組み（参考）
- `share/MGL-01.html`〜`MGL-16.html` … タイプ別シェアページ。OGP画像を持ち、開くと診断トップへ転送。
- `assets/share/ogp/MGL-XX.png` … X・LINEのリンクカードに出る画像（1200×630）。
- X/LINEでシェアすると、このページのOGP画像がカードとして表示され、**カードを押した人は診断トップ**に来ます。

> ⚠️ 公開ドメインを `shindan.mingleapp.jp` 以外にする場合は、
> `share/*.html` 内と `index.html` のOGP URL（`shindan.mingleapp.jp`）を実ドメインに置換してください。
> （Claude Codeに「ドメインを◯◯に置換して」と頼めば一括で直せます）

## 困ったら
リリース後の文言・デザイン・計測の修正は、この `release` フォルダを **Claude Code** に渡せば対応できます。修正後は再アップロードするだけで反映されます。
