# Mingle 友達タイプ診断 — 公開パッケージ

このフォルダの中身を、そのままWebサーバー（静的ホスティング）に置けば公開できます。

## 公開手順（3ステップ）

### 1. 登録メールの集め方を決める（重要）
結果ページの「登録して続きを読む」フォームで集めたメールアドレスを、どこに届けるか設定します。
`config.js` に `subscribeEndpoint` を追記してください。
```js
window.MINGLE_CONFIG = {
  registerUrl:  "https://mingleapp.jp",  // 結果末尾「事前登録する」ボタンの遷移先
  diagnoseUrl:  "https://mingleapp.jp",  // この診断ページのURL（QR・シェアに使用）
  subscribeEndpoint: "https://（フォーム/API のURL）"  // ← メール送信先（任意）
};
```
- **未設定のまま**だと、メールは送信側ブラウザの `localStorage` に保存されるだけで手元には届きません（デモ動作）。
- 実際に集めるには、フォーム受信サービス（例：Formspree / Google Forms / Supabase / 自前API など）のURLを `subscribeEndpoint` に入れてください。`{ email, type }` を JSON で POST します。
- ※ `registerUrl`（既定 `https://mingleapp.jp`）は結果末尾のCTAの遷移先です。

### 2. OGP画像を絶対URLにする
`index.html` の中の2か所（`og:image` と `twitter:image`）を、ドメイン付きの絶対URLに変えます。
```html
<meta property="og:image" content="https://（あなたのドメイン）/assets/share/ogp-default.png">
<meta name="twitter:image" content="https://（あなたのドメイン）/assets/share/ogp-default.png">
```
※ X・LINEのリンクカードは絶対URLでないと表示されません。

### 3. アップロードする
このフォルダの中身を丸ごと、静的ホスティング（例：Cloudflare Pages / Netlify / Vercel / Firebase Hosting / S3+CloudFront など）にアップロードするだけ。`index.html` が入口です。独自ドメインを割り当てれば完成。

---

## 動作する機能（サーバー不要）
- ランディング → 12問 → 分析演出 → 結果（全16タイプ判定）
- 結果ページ：登場演出・相性タイプ遷移
- **取扱説明書の解除：メールアドレスを入力して送信すると、その場で続き（取扱説明書）が開く**
- 結果末尾「事前登録する」 → 事前登録ページ（mingleapp.jp）を開く
- 「シェア」ボタン → X・LINEへ投稿／結果画像を保存（スマホはそのままInstagramストーリーズへ）

## ファイル構成
```
index.html          入口（診断フロー＋OGP設定）
config.js           ★差し替え：事前登録URL・診断URL・メール送信先
types-data.js       16タイプの全コンテンツ
result-page.js      結果ページ描画・メール登録ゲート・シェア
result-page.css     スタイル（結果・登録ゲート・CTA）
flow.js             設問・スコアリング・画面遷移
flow.css            ランディング・設問・分析のスタイル
share-image.js      シェア画像（OGP/ストーリーズ）生成
types.html          16タイプ一覧ページ
assets/characters/  16キャラ画像（差し替え可：char_MGL-01〜16.png）
assets/icons/       X・LINEアイコン
assets/share/       OGPデフォルト画像
```

## 任意（拡散をさらに強くしたい場合）
X・LINEで**結果ごとに違うリンクカード**を出したい場合は、各タイプ専用URL＋その`og:image`が必要です（タイプ別OGP16枚は「シェア画像スタジオ」で書き出せます）。今のままでも、ユーザーが画像を保存して投稿する導線は完全に機能します。

## 注意
- フォント（Zen Maru / Zen Kaku / Outfit）とシェア機能の一部はCDNを読み込みます。社内ネットワーク制限がある場合はセルフホストに切り替えてください。
- 画像を差し替える場合は同じファイル名・正方形(透過PNG)推奨。
